# MetaHuman FBX Validator - REQUIRED Dependencies
#
# ALL dependencies are REQUIRED - no fallbacks or alternatives
# Tool will crash immediately if any dependency is missing

# Rich console library - REQUIRED (no fallback console implementation)
rich>=13.0.0
